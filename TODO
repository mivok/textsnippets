Add daemon option
    - automatically log errors to a file
    - perhaps have some way of logging to a window?
        - Log to in memory file (stringIO)
        - Show a textbox containing this file when an error occurs

Logging
    - file to output to
    - Perhaps a gui messages viewer

Setuptools
    - Package things ready for release

Config for the window
    - colors/gradients etc.
    - need to make sure the color spec is consistent in the config file
    - html hex colors

Support for typing special keys
    - newlines
    - Tab
    - modifiers
    - F keys
