Add options
    - -d daemon
    - config file location
    - debug mode

Add daemon option
    - automatically log errors to a file
    - perhaps have some way of logging to a window?
        - Log to in memory file (stringIO)
        - Show a textbox containing this file when an error occurs

Logging
    - log level
    - file to output to

Add a post-correct hotkey and autocorrection:
    - This will grab keys one by one, using the keytree code that was previously
    removed (<3 version control)
    - Type backspace n times in order for it to delete the old keys

Support for typing special keys
    - newlines
    - Tab
    - modifiers
    - F keys
